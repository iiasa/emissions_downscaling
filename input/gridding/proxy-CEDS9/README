README
This folder is where the gridding proxy (CEDS9) profiles are stored.

The only documents in this folder that are versioned are this README document
and .gitignore file, which tells git to ignore changes to all other files in
this folder.
